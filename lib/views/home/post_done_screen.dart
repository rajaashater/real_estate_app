import 'package:flutter/material.dart';

class PostDoneScreen extends StatelessWidget {
  const PostDoneScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
