import 'package:flutter/cupertino.dart';

class KTextFormField extends StatelessWidget {
  const KTextFormField({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
